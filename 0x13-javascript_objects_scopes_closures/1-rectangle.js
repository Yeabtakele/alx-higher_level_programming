#!/usr/bin/node
// Write an empty class Rectangle that defines a rectangle
const Rectangle = require('./1-rectangle'); {
   constructor (w, h) {
       this.width = w;
           this.height = h;
             }
             };
